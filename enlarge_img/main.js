const img = document.querySelector('img');
const large = document.querySelector('.large');

img.addEventListener('click', enlarge);

function enlarge() {
  // img.style.height = '500px';
  large.appendChild.createClass(large);
}
